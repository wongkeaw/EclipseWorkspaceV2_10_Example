package com.example.test3;

import java.util.ArrayList;
import java.util.Arrays;
import java.util.Comparator;
import java.util.List;
import java.util.stream.Collectors;

public class test05 {

	public test05() {
		// TODO Auto-generated constructor stub
	}

	public static void main(String[] args) {
		// TODO Auto-generated method stub
		int[] i = {1,2,3,4,6,7,8,9,10} ;
		int b = new sulution05().sulution(i);
		System.out.println(b);
	}

}
class sulution05{
	// O(n)
	public int sulution(int[] i) {
		int[] ints = i.clone() ;
		// convert int[] ==> List<Integer>
		@SuppressWarnings("serial")
		//List<Integer> ai = new ArrayList<Integer>() {{ for (int k : ints) add(k); }} ; 
		List<Integer> ai = Arrays.stream(ints).boxed().collect(Collectors.toList());
		ai.sort(Comparator.naturalOrder());
		int x = 0 ;
		for (int j = 0; j < i.length; j++) {
			System.out.println("index at: "+j+" Original value : "+i[j]  +" Order by Value : "+ai.get(j));
			// fix = 2 : 1 swap have 2 change position
			if (  i[j] != ai.get(j) && ++x > 2  ) return 0 ;
		}
		System.out.println(x);
		return x ;
	}
}